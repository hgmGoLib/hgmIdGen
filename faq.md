# FAQ — 对一次设计 review 的逐条回复

本文回复一次针对 hgmIdGen 的设计 review 提问.
说明当前的设计取舍和边界, **不改任何代码**.
配套细节见 [`doc/implDetail.md`](doc/implDetail.md) (二进制布局) 与 [`doc/edgeCase.md`](doc/edgeCase.md) (极限情况).

结论先行:review 提到的绝大多数点都是**已知的、有意的取舍**,不是遗漏.下面逐条说明为什么现在这样.

---

## 一、NewSecureId 的时间精度与 counter

### Q: NewSecureId 需要毫秒级时间吗?改成秒级/分钟级时间桶是不是更合适?

保持毫秒级.理由:

* 安全性不靠时间段,靠随机段.NewSecureId 的随机段有 16\~19 字节(128\~152 bit),这才是"猜不到未使用 token"的来源.把时间桶变粗**不会增加任何安全性**,因为时间从来不是秘密.
* 毫秒时间换来两个实打实的好处:字符串可按时间排序、`MinSecureIdAtTime()` 能做时间范围扫描下界.粗时间桶会牺牲这两点.
* 和 NewId 结构统一,一套代码、一套心智模型.

换句话说:时间桶变粗是"用确定的功能损失,换一个并不存在的安全收益",不划算.

### Q: NewSecureId 需要暴露同毫秒内的 counter 吗?secure 版本是不是只留时间桶 + 随机就够?

保留 counter.理由:

* counter 的作用是**同一毫秒内确定性去重**.只靠随机段去重是概率性的;有了 counter,同进程同毫秒内的每个 id 递增段天然唯一,碰撞只可能发生在随机段,双保险.
* 关于"counter 会泄露 qps"的顾虑:我方时间精度是 1 毫秒,攻击者从 counter 至多能推算"某毫秒内本进程生成了几个 id",拿不到一秒内的分布、也拿不到跨进程的总量.信息量很小,不构成实际威胁.
* 所以这里的取舍是:用极少量的 qps 侧信道,换同毫秒确定性去重.值得.

> 如果哪天真的要堵这个侧信道,正确做法不是"去掉 counter",而是"换一个不可排序的纯随机 token 类型"(见本文最后一条),而不是削弱现有 NewSecureId.

### Q: NewSecureId 的定位到底是"可排序安全 token",还是"更长的普通 ID"?

是**"带时间前缀、可排序的安全 token"**,不是"纯不透明随机串".准确表述:

* 它和 NewId 是同一个三段式结构(时间 + 递增 + 随机),只是随机段更长.
* 它"安全"的含义是:**攻击者无法猜出一个合法且尚未分配的 token**(随机段 128+ bit 兜底),适合 sessionId / accessToken.
* 它**不**保证"零信息泄露":时间段可反解(这是特性,不是 bug),counter 可推算(见上).如果你的威胁模型要求 token 本身不含任何可推断信息,那是另一种需求,当前库不覆盖(见最后一条).

---

## 二、IsValidId / ParseTimeFromId 的语义

### Q: IsValidId 的语义是"格式合法",还是"本库规范编码合法"?

只是**"格式合法"**:长度对(29) + 每个字符都在 base32 字符表内.

它是一道**廉价的语法门**,故意做得轻:
* 不解码、不校验时间是否合理、不校验递增段结构.
* **不能**证明这个字符串一定是本库生成的,也不能证明它是 canonical 编码.

需要更强保证时请往上走一档:`IdToIdBinary()` 会真正做 base32 解码到 18 字节,长度或字符非法会返回 error.

### Q: IsValidId 需不需要 decode 再 encode,检查 canonical?

当前**故意不做** canonical 检查.但这里有个**必须写清楚的真实边界**(已实测确认):

* 29 字符承载 145 bit,而二进制只有 144 bit,末字符有 **1 个 trailing bit 是冗余的**.
* Go 的 `encoding/base32` 解码器**不校验这个 trailing bit**.实测:`"…1"` 和 `"…2"` 两个不同字符串会解码成**同一个** 18 字节二进制.
* 后果:一个非 canonical 的变体字符串**能通过 IsValidId**,并且 decode 出和 canonical 串一样的 binary.

因此:
* 如果你**只存/比较本库自己产出的字符串**(它们都是 canonical 的),不会遇到这个问题——一一对应成立.
* 如果你可能拿到**外部构造**的字符串并且**直接拿字符串做主键/比较**,要么先 `IdToIdBinary()` 归一到二进制再比较/存储,要么自行加 canonical 校验.
* 这也是我们**不承诺 IsValidId 做 canonical**的原因:它定位是廉价门,canonical 归一化的责任交给 `IdToIdBinary()`.

### Q: ParseTimeFromId 面对外部乱输入,应该严格返回 error 吗?

保持现在的约定:**失败返回 `time.Time{}` 零值,不返回 error**.理由:

* 全库时间解析函数统一用"零值即失败"的哨兵约定(`ParseTimeFromId` / `ParseTimeFromSecureId` 都一样),调用方 `t.IsZero()` 判断即可,API 更简单.
* 需要严格拒绝外部输入时,先 `IsValidId()` 过一道再解析,职责分离清楚:校验归校验,解析归解析.
* 结论:不改成 error.这是一致性取舍,不是漏掉了错误处理.

---

## 三、counter 与变长编码

### Q: counter 变长编码的主要目的是什么:省长度、增加随机位,还是排序?

主要目的是**在固定总长度下,让常见情况(counter 较小)拿到尽可能长的随机段**——也就是"省长度 → 把省下的字节让给随机".

* 总长度固定(NewId 18 字节).若 counter 用固定 4 字节,随机段永远只有 8 字节;变长后 counter 绝大多数时候只占 1 字节,随机段有 11 字节,常见情况多 3 字节熵.
* 排序是**顺带保住的性质,不是目的**:变长前缀的高 2 位随数值单调递增,所以变长不破坏排序(见 [`doc/edgeCase.md`](doc/edgeCase.md) 第 4 条),但这是约束、不是动机.

优先级:增加常见情况随机位 > 固定总长度 > (顺带)不破坏排序.

### Q: counter 溢出后,文档要不要明确写"不保证同毫秒内顺序"?

要,而且已经写了.明确表述:

> 同一进程同一毫秒内调用超过 `2^30`(约 10.7 亿)次时,递增段高 2 位被掩码回绕,**此毫秒内不再保证递增顺序,也不再保证递增段唯一**,只能靠随机段兜底防碰撞.

现实中同毫秒 10 亿次基本不可能达到,但压测紧凑循环、不推进时间时有理论可能.详见 [`doc/edgeCase.md`](doc/edgeCase.md) 第 1 条.

### Q: 时间回拨不处理,文档要不要明确写"依赖 wall clock,不保证强单调"?

要,已经写了.明确表述:

> 本库时间取自 `time.Now()`(wall clock),**不保证强单调**.NTP/手动改时间导致时钟回拨时,"递增"和"字符串排序==时间排序"两条性质都会被破坏;被回拨命中的毫秒里递增值可能与之前重复,是否碰撞完全交给随机段.

需要严格全局单调的场景,不要假设本库能对抗时钟回拨.详见 [`doc/edgeCase.md`](doc/edgeCase.md) 第 3 条.

---

## 四、随机源与并发

### Q: randFast 该用于 NewSecureId 吗,还是 secure 版本每次直接 crypto/rand.Read?

用 randFast 是可以的,对 NewSecureId 也**不削弱安全性**:

* randFast 的字节全部来自 `crypto/rand`,只是一次读 512 字节缓存起来、之后**每个字节只被消费一次**顺序取出.它没有复用随机字节,熵不因缓存下降,只是省了 `rand.Read` 的调用次数.
* 源码注释里的"安全性未知"指的是**这层批量封装本身没做过正式安全审计**,不是说随机源被换弱了.
* 结论:功能上等价于每次直接 `crypto/rand.Read`,保留现状.若某天要追求"零疑虑",可以给 secure 路径切直连 `crypto/rand`,但那是可选优化,不是必需修复.

### Q: 全局 mutex 的目标吞吐是多少?需要并发 benchmark 吗?

坦白说:**当前没有设定量化吞吐目标,也还没有并发 benchmark**.现状说明:

* 有两把锁,临界区都极小:`nextId` 的计数器锁、`randFast` 的取随机锁.
* 设计定位是"普通应用生成主键/token 的速率",离锁竞争瓶颈很远,所以一直没有立目标值.
* 如果实测某条热路径确实卡在这把全局锁上,再考虑 per-P 分片计数器 / 分片随机池;在此之前不做过度设计.
* 补一个并发 benchmark 是合理的 nice-to-have,但不阻塞现有用法.**这里不夸大,如实说明:未测.**

---

## 五、命名、稳定性、唯一性、纯随机 token

### Q: 要不要补 Go 风格别名 NewID / NewSecureID / IDToBinary?

**不补.** 维持 `NewId` / `NewSecureId` / `IdToIdBinary`.

命名原则(本项目一贯做法):一个词一个词地拼,要能一眼读出每个单词.`ID` 这种全大写缩写反而不像一个"词",可读性更差;所谓 Go 风格的全大写缩写这里不采用.驼峰法在本项目里是首字母大写、逐词拼接,字段名/类型名/JSON 名全程 ascii 一致,不做大小写缩写转换.所以不会为了迎合 Go 惯例加 `ID` 别名.

### Q: ID 格式承诺长期稳定吗?能作为数据库长期存储格式吗?

**承诺稳定,可以长期存库.** 三个格式是稳定契约:

* NewId → 29 字符 / 18 字节二进制
* NewSecureId → 42 字符 / 26 字节二进制
* 二进制三段式布局 + base32 字符表固定不变

存字符串(canonical)或存 18/26 字节二进制都可以.二进制更省空间,字符串更直观且可直接排序.

### Q: 自定义 base32 保证任意合法字符串都只有唯一 binary 对应吗?

**不保证反向唯一.** 精确表述(实测确认):

* 正向(binary → 字符串)是确定且唯一的:本库产出的都是 canonical 串.
* 反向(字符串 → binary)**不是一一对应**:末字符有 1 个冗余 trailing bit,`"…1"` 与 `"…2"` 会解码成同一个 binary(见前面 IsValidId 那条).
* 实践准则:**只用本库产出的字符串**时,一一对应成立;若会接收外部构造的字符串,先 `IdToIdBinary()` 归一到二进制再比较/存储.

### Q: 需要提供纯随机 token API,和可排序 secure ID 分开吗?

当前**没有**这种 API,如实承认这是一个覆盖缺口:

* NewSecureId 是"可排序 + 带时间前缀"的安全 token,天生泄露时间(特性).
* 如果某个威胁模型要求 token **不含任何可推断信息**(不排序、不含时间、不含 counter),那确实是另一类需求,直接 `crypto/rand.Read` 自己编码即可,或将来作为一个独立类型加入本库.
* **这里不改代码**,只记录:这是一个有意的边界,不是遗漏.真有需求时再单独开一个纯随机 token 类型,而不是去污染 NewSecureId 的语义.
